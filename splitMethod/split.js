
const text = 'I love Eat Apples'
console.log(text)

const cuttedText = text.split(' ')
console.log(cuttedText) 

const joinnedText = cuttedText.join(' ')
console.log(joinnedText)

/**### Quando precisamos separar palavras em um texto. Podemos usar o método split() e dentro do parâmetro, qual serão as referencias de separação. Se serão por espaço ou por certas letras.

### E quando precisamos juntar palavras separadas em um array por exemplo. Podemos usar o método join() para juntar as palavras e dentro dos parâmetros as referencias de junção. para cada palavra um espaço ou uma virgula por exemplo. */

